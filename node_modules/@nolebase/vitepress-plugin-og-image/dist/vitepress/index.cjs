'use strict';

const node_path = require('node:path');
const posix = require('node:path/posix');
const node_url = require('node:url');
const fs = require('fs-extra');
const glob = require('glob');
const colorette = require('colorette');
const GrayMatter = require('gray-matter');
const unified = require('unified');
const RehypeMeta = require('rehype-meta');
const RehypeParse = require('rehype-parse');
const RehypeStringify = require('rehype-stringify');
const ora = require('ora');
const resvgJs = require('@resvg/resvg-js');
const regexCreator = require('emoji-regex');

var _documentCurrentScript = typeof document !== 'undefined' ? document.currentScript : null;
function _interopDefaultCompat (e) { return e && typeof e === 'object' && 'default' in e ? e.default : e; }

const fs__default = /*#__PURE__*/_interopDefaultCompat(fs);
const GrayMatter__default = /*#__PURE__*/_interopDefaultCompat(GrayMatter);
const RehypeMeta__default = /*#__PURE__*/_interopDefaultCompat(RehypeMeta);
const RehypeParse__default = /*#__PURE__*/_interopDefaultCompat(RehypeParse);
const RehypeStringify__default = /*#__PURE__*/_interopDefaultCompat(RehypeStringify);
const ora__default = /*#__PURE__*/_interopDefaultCompat(ora);
const regexCreator__default = /*#__PURE__*/_interopDefaultCompat(regexCreator);

function getLocales(siteData) {
  const locales = [];
  locales.push(siteData.lang ?? "root");
  if (Object.keys(siteData.locales).length === 0)
    return locales;
  for (const locale in siteData.locales) {
    if (locale !== siteData.lang)
      locales.push(locale);
  }
  return locales;
}
function getTitleWithLocales(siteData, locale) {
  if (Object.keys(siteData.locales).length > 0) {
    const title = siteData.locales[locale]?.title;
    if (title)
      return title;
    if (siteData.locales.root.title)
      return siteData.locales.root.title;
    return siteData.title;
  }
  return siteData.title;
}
function getDescriptionWithLocales(siteData, locale) {
  if (Object.keys(siteData.locales).length > 0) {
    const description = siteData.locales[locale]?.description;
    if (description)
      return description;
    if (siteData.locales.root.description)
      return siteData.locales.root.description;
    return siteData.description;
  }
  return siteData.description;
}

function getSidebar(siteData, themeConfig) {
  const locales = getLocales(siteData);
  if (locales.length === 0) {
    return {
      defaultLocale: siteData.lang,
      locales: locales || [],
      sidebar: {
        [siteData.lang]: flattenThemeConfigSidebar(themeConfig.sidebar) || []
      }
    };
  }
  const sidebar = {
    defaultLocale: siteData.lang,
    locales,
    sidebar: {}
  };
  for (const locale of locales) {
    let themeConfigSidebar = [];
    if (typeof siteData.locales[locale]?.themeConfig?.sidebar !== "undefined")
      themeConfigSidebar = siteData.locales[locale]?.themeConfig?.sidebar || [];
    else if (typeof siteData.themeConfig?.sidebar !== "undefined")
      themeConfigSidebar = siteData.themeConfig?.sidebar || [];
    else if (typeof themeConfig.sidebar !== "undefined")
      themeConfigSidebar = themeConfig.sidebar;
    else
      themeConfigSidebar = [];
    sidebar.sidebar[locale] = flattenThemeConfigSidebar(themeConfigSidebar) || [];
  }
  return sidebar;
}
function flattenThemeConfigSidebar(sidebar) {
  if (!sidebar)
    return [];
  if (Array.isArray(sidebar))
    return sidebar;
  return Object.keys(sidebar).reduce((prev, curr) => {
    const items = sidebar[curr];
    return prev.concat(items);
  }, []);
}
function flattenSidebar(sidebar) {
  return sidebar.reduce((prev, curr) => {
    if (curr.items)
      return prev.concat(flattenSidebar(curr.items));
    return prev.concat(curr);
  }, []);
}

const okMark = colorette.green("\u2713");
const failMark = colorette.red("\u2716");
async function task(taskName, task2) {
  const startsAt = Date.now();
  const moduleNamePrefix = colorette.cyan("@nolebase/vitepress-plugin-og-image");
  const grayPrefix = colorette.gray(":");
  const spinnerPrefix = `${moduleNamePrefix}${grayPrefix}`;
  const spinner = ora__default({ discardStdin: false });
  spinner.start(`${spinnerPrefix} ${taskName}...`);
  let result;
  try {
    result = await task2();
  } catch (e) {
    spinner.stopAndPersist({ symbol: failMark });
    throw e;
  }
  const elapsed = Date.now() - startsAt;
  const suffixText = `${colorette.gray(`(${elapsed}ms)`)} ${result}` ?? "";
  spinner.stopAndPersist({ symbol: okMark, suffixText });
}
function renderTaskResultsSummary(results, siteConfig) {
  const successCount = results.filter((item) => item.status === "success");
  const skippedCount = results.filter((item) => item.status === "skipped");
  const erroredCount = results.filter((item) => item.status === "errored");
  const stats = `${colorette.green(`${successCount.length} generated`)}, ${colorette.yellow(`${skippedCount.length} skipped`)}, ${colorette.red(`${erroredCount.length} errored`)}`;
  const skippedList = ` - ${colorette.yellow("Following files were skipped")}:

${skippedCount.map((item) => {
    return colorette.gray(`    - ${node_path.relative(siteConfig.root, item.filePath)}: ${item.reason}`);
  }).join("\n")}`;
  const erroredList = ` - ${colorette.red("Following files encountered errors")}

${erroredCount.map((item) => {
    return colorette.gray(`    - ${node_path.relative(siteConfig.root, item.filePath)}: ${item.reason}`);
  }).join("\n")}`;
  const overallResults = [stats];
  if (skippedCount.length > 0)
    overallResults.push(skippedList);
  if (erroredCount.length > 0)
    overallResults.push(erroredList);
  return overallResults.join("\n\n");
}

const emojiRegex = regexCreator__default();
function removeEmoji(str) {
  return str.replace(emojiRegex, "");
}

const escapeMap = {
  "<": "&lt;",
  ">": "&gt;",
  "'": "&apos;",
  '"': "&quot;",
  "&": "&amp;"
};
function escape(content, ignore) {
  ignore = (ignore || "").replace(/[^&"<>\']/g, "");
  const pattern = `([&"<>'])`.replace(new RegExp(`[${ignore}]`, "g"), "");
  return content.replace(new RegExp(pattern, "g"), (_, item) => {
    return escapeMap[item];
  });
}

function templateSVG(siteName, siteDescription, title, category, ogTemplate) {
  const lines = removeEmoji(title).trim().replace(/(?![^\n]{1,17}$)([^\n]{1,17})\s/g, "$1\n").split("\n");
  lines.forEach((val, i) => {
    if (val.length > 17) {
      lines[i] = val.slice(0, 17);
      lines[i + 1] = `${val.slice(17)}${lines[i + 1] || ""}`;
    }
    lines[i] = lines[i].trim();
  });
  const categoryStr = category ? removeEmoji(category).trim() : "";
  const data = {
    siteName,
    siteDescription,
    category: categoryStr,
    line1: lines[0] || "",
    line2: lines[1] || "",
    line3: `${lines[2] || ""}${lines[3] ? "..." : ""}`
  };
  return ogTemplate.replace(/\{\{([^}]+)}}/g, (_, name) => {
    if (!name || typeof name !== "string" || !(name in data))
      return "";
    const nameKeyOf = name;
    return escape(data[nameKeyOf]);
  });
}
async function renderSVG(svgContent, options) {
  let resvg;
  try {
    resvg = new resvgJs.Resvg(svgContent, {
      fitTo: { mode: "width", value: 1200 },
      font: {
        fontFiles: options?.fontPath ? [options.fontPath] : [],
        // Load system fonts might cost more time
        loadSystemFonts: false
      }
    });
  } catch (err) {
    throw new Error(`Failed to initiate Resvg instance to render open graph images due to ${err}`);
  }
  try {
    return resvg.render().asPng();
  } catch (err) {
    throw new Error(`Failed to render open graph images on path due to ${err}`);
  }
}

const logModulePrefix = `${colorette.cyan(`@nolebase/vitepress-plugin-og-image`)}${colorette.gray(":")}`;
async function tryToLocateTemplateSVGFile(siteConfig) {
  const templateSvgPathUnderPublicDir = node_path.resolve(siteConfig.root, "public", "og-template.svg");
  if (await fs__default.pathExists(templateSvgPathUnderPublicDir))
    return templateSvgPathUnderPublicDir;
  const __dirname = node_path.dirname(node_url.fileURLToPath((typeof document === 'undefined' ? require('u' + 'rl').pathToFileURL(__filename).href : (_documentCurrentScript && _documentCurrentScript.src || new URL('vitepress/index.cjs', document.baseURI).href))));
  const templateSvgPathUnderRootDir = node_path.resolve(__dirname, "assets", "og-template.svg");
  if (await fs__default.pathExists(templateSvgPathUnderRootDir))
    return templateSvgPathUnderRootDir;
  return void 0;
}
async function tryToLocateFontFile(siteConfig) {
  const fontPathUnderPublicDir = node_path.resolve(siteConfig.root, "public", "SourceHanSansSC.otf");
  if (await fs__default.pathExists(fontPathUnderPublicDir))
    return fontPathUnderPublicDir;
  const __dirname = node_path.dirname(node_url.fileURLToPath((typeof document === 'undefined' ? require('u' + 'rl').pathToFileURL(__filename).href : (_documentCurrentScript && _documentCurrentScript.src || new URL('vitepress/index.cjs', document.baseURI).href))));
  const fontPathUnderRootDir = node_path.resolve(__dirname, "assets", "SourceHanSansSC.otf");
  if (await fs__default.pathExists(fontPathUnderRootDir))
    return fontPathUnderRootDir;
  return void 0;
}
async function renderSVGAndRewriteHTML(siteConfig, siteTitle, siteDescription, page, file, ogImageTemplateSvg, ogImageTemplateSvgPath, domain) {
  const fileName = node_path.basename(file, ".html");
  const ogImageFilePathBaseName = `og-${fileName}.png`;
  const ogImageFilePathFullName = `${node_path.dirname(file)}/${ogImageFilePathBaseName}`;
  const templatedOgImageSvg = templateSVG(
    siteTitle,
    siteDescription,
    page.title,
    page.category ?? "",
    ogImageTemplateSvg
  );
  try {
    await renderSVGAndSavePNG(
      templatedOgImageSvg,
      ogImageFilePathFullName,
      ogImageTemplateSvgPath,
      node_path.relative(siteConfig.root, file),
      { fontPath: await tryToLocateFontFile(siteConfig) }
    );
  } catch (err) {
    return {
      filePath: file,
      status: "errored",
      reason: String(err)
    };
  }
  const html = await fs__default.readFile(file, "utf-8");
  const result = await unified.unified().use(RehypeParse__default).use(RehypeMeta__default, {
    og: true,
    twitter: true,
    name: siteTitle,
    description: siteDescription,
    image: {
      url: `${domain}/${encodeURIComponent(node_path.relative(siteConfig.outDir, ogImageFilePathFullName))}`
    }
  }).use(RehypeStringify__default).process(html);
  try {
    await fs__default.writeFile(file, String(result), "utf-8");
  } catch (err) {
    console.error(
      `${logModulePrefix} `,
      `${colorette.red("[ERROR] \u2717")} failed to write transformed HTML on path [${node_path.relative(siteConfig.root, file)}] due to ${err}`,
      `
${colorette.red(err.message)}
${colorette.gray(String(err.stack))}`
    );
    return {
      filePath: file,
      status: "errored",
      reason: String(err)
    };
  }
  return {
    filePath: file,
    status: "success"
  };
}
async function renderSVGAndSavePNG(svgContent, saveAs, forSvgSource, forFile, options) {
  let pngBuffer;
  try {
    pngBuffer = await renderSVG(svgContent, { fontPath: options.fontPath });
  } catch (err) {
    console.error(
      `${logModulePrefix} `,
      `${colorette.red("[ERROR] \u2717")} failed to generate open graph image as ${colorette.green(`[${saveAs}]`)} with ${colorette.green(`[${forSvgSource}]`)} due to ${colorette.red(String(err))}`,
      `skipped open graph image generation for ${colorette.green(`[${forFile}]`)}`,
      `

SVG Content:

${svgContent}`,
      `

Detailed stack information bellow:

${colorette.red(err.message)}
${colorette.gray(String(err.stack))}`
    );
    throw err;
  }
  try {
    await fs__default.writeFile(saveAs, pngBuffer, "binary");
  } catch (err) {
    console.error(
      `${logModulePrefix} `,
      `${colorette.red("[ERROR] \u2717")} open graph image rendered successfully, but failed to write generated open graph image on path [${saveAs}] due to ${err}`,
      `
${colorette.red(err.message)}
${colorette.gray(String(err.stack))}`
    );
    throw err;
  }
}
async function applyCategoryText(pageItem, categoryOptions) {
  if (typeof categoryOptions?.byCustomGetter !== "undefined") {
    const gotTextMaybePromise = categoryOptions.byCustomGetter({ ...pageItem });
    if (typeof gotTextMaybePromise === "undefined")
      return void 0;
    if (gotTextMaybePromise instanceof Promise)
      return await gotTextMaybePromise;
    if (gotTextMaybePromise)
      return gotTextMaybePromise;
    return void 0;
  }
  if (typeof categoryOptions?.byPathPrefix !== "undefined") {
    for (const { prefix, text } of categoryOptions.byPathPrefix) {
      if (pageItem.normalizedSourceFilePath.startsWith(prefix)) {
        if (!text) {
          console.warn(
            `${logModulePrefix} ${colorette.yellow("[WARN]")} empty text for prefix ${prefix} when processing ${pageItem.sourceFilePath} with categoryOptions.byPathPrefix, will ignore...`
          );
          return void 0;
        }
        return text;
      }
      if (pageItem.normalizedSourceFilePath.startsWith(`/${prefix}`)) {
        if (!text) {
          console.warn(
            `${logModulePrefix} ${colorette.yellow("[WARN]")} empty text for prefix ${prefix} when processing ${pageItem.sourceFilePath} with categoryOptions.byPathPrefix, will ignore...`
          );
          return void 0;
        }
        return text;
      }
    }
    console.warn(
      `${logModulePrefix} ${colorette.yellow("[WARN]")} no path prefix matched for ${pageItem.sourceFilePath} with categoryOptions.byPathPrefix, will ignore...`
    );
    return void 0;
  }
  if (typeof categoryOptions?.byLevel !== "undefined") {
    const level = Number.parseInt(String(categoryOptions?.byLevel ?? 0));
    if (Number.isNaN(level)) {
      console.warn(
        `${logModulePrefix} ${colorette.yellow("[ERROR]")} byLevel must be a number, but got ${categoryOptions.byLevel} instead when processing ${pageItem.sourceFilePath} with categoryOptions.byLevel, will ignore...`
      );
      return void 0;
    }
    const dirs = pageItem.sourceFilePath.split(node_path.sep);
    if (dirs.length > level)
      return dirs[level];
    console.warn(`${logModulePrefix} ${colorette.red(`[ERROR] byLevel is out of range for ${pageItem.sourceFilePath} with categoryOptions.byLevel.`)} will ignore...`);
    return void 0;
  }
  return void 0;
}
async function applyCategoryTextWithFallback(pageItem, categoryOptions) {
  const customText = await applyCategoryText(pageItem, categoryOptions);
  if (customText)
    return customText;
  const fallbackWithFrontmatter = typeof categoryOptions?.fallbackWithFrontmatter === "undefined" ? true : categoryOptions.fallbackWithFrontmatter;
  if (fallbackWithFrontmatter && "category" in pageItem.frontmatter && pageItem.frontmatter.category && typeof pageItem.frontmatter.category === "string")
    return pageItem.frontmatter.category ?? "";
  console.warn(`${logModulePrefix} ${colorette.yellow("[WARN]")} no category text found for ${pageItem.sourceFilePath} with categoryOptions ${JSON.stringify(categoryOptions)}.}`);
  return "Un-categorized";
}
function buildEndGenerateOpenGraphImages(options) {
  return async (siteConfig) => {
    const ogImageTemplateSvgPath = await tryToLocateTemplateSVGFile(siteConfig);
    await task("rendering open graph images", async () => {
      const themeConfig = siteConfig.site.themeConfig;
      const sidebar = getSidebar(siteConfig.site, themeConfig);
      let pages = [];
      for (const locale of sidebar.locales) {
        const flattenedSidebar = flattenSidebar(sidebar.sidebar[locale]);
        const items = [];
        for (const item of flattenedSidebar) {
          const relativeLink = item.link ?? "";
          const sourceFilePath = relativeLink.endsWith("/") ? `${relativeLink}index.md` : `${relativeLink}.md`;
          const sourceFileContent = fs__default.readFileSync(`${node_path.join(siteConfig.root, sourceFilePath)}`, "utf-8");
          const { data } = GrayMatter__default(sourceFileContent);
          const res = {
            ...item,
            title: item.text ?? item.title ?? "Untitled",
            category: "",
            locale,
            frontmatter: data,
            sourceFilePath,
            normalizedSourceFilePath: sourceFilePath.split(node_path.sep).join(posix.sep)
          };
          res.category = await applyCategoryTextWithFallback(res, options.category);
          items.push(res);
        }
        pages = pages.concat(items);
      }
      const files = await glob.glob(`${siteConfig.outDir}/**/*.html`, { nodir: true });
      if (!ogImageTemplateSvgPath) {
        return `${colorette.green(`${0} generated`)}, ${colorette.yellow(`${files.length} (all) skipped`)}, ${colorette.red(`${0} errored`)}.

 - ${colorette.red("Failed to locate")} og-template.svg ${colorette.red("under public or plugin directory")}, did you forget to put it? will skip open graph image generation.`;
      }
      const ogImageTemplateSvg = fs__default.readFileSync(ogImageTemplateSvgPath, "utf-8");
      const generatedForFiles = await Promise.all(files.map(async (file) => {
        const relativePath = node_path.relative(siteConfig.outDir, file);
        const link = `/${relativePath.slice(0, relativePath.lastIndexOf(".")).replaceAll(node_path.sep, "/")}`.split("/index")[0];
        const page = pages.find((item) => {
          if (item.link === link)
            return true;
          if (item.link === `${link}/`)
            return true;
          return false;
        });
        if (!page) {
          return {
            filePath: file,
            status: "skipped",
            reason: "correspond Markdown page not found in sidebar"
          };
        }
        const siteTitle = getTitleWithLocales(siteConfig.site, page.locale);
        const siteDescription = getDescriptionWithLocales(siteConfig.site, page.locale);
        return await renderSVGAndRewriteHTML(
          siteConfig,
          siteTitle,
          siteDescription,
          page,
          file,
          ogImageTemplateSvg,
          ogImageTemplateSvgPath,
          options.baseUrl
        );
      }));
      return renderTaskResultsSummary(generatedForFiles, siteConfig);
    });
  };
}

exports.buildEndGenerateOpenGraphImages = buildEndGenerateOpenGraphImages;
//# sourceMappingURL=index.cjs.map
