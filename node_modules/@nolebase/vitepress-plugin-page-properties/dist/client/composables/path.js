"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useRawPath = useRawPath;
var _vue = require("vue");
var _vitepress = require("vitepress");
function useRawPath() {
  const route = (0, _vitepress.useRoute)();
  return (0, _vue.computed)(() => {
    let path = decodeURIComponent(route.path).toLowerCase();
    if (path.startsWith("/")) path = path.slice(1);
    if (path.endsWith("/")) path += "index.md";else path = path.replace(/^(.+?)(\.html)?$/s, "$1.md");
    return path.toLowerCase();
  });
}