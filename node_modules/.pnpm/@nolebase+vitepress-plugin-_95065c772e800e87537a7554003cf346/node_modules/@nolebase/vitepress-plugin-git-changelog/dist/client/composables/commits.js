"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useCommits = useCommits;
var _vue = require("vue");
function useCommits(allCommits, path) {
  return (0, _vue.computed)(() => {
    const currentPath = (0, _vue.toValue)(path);
    const commits = allCommits.filter(c => c.path === currentPath) || [];
    return commits.filter((commit, index) => {
      if (commit.tag && (!commits[index + 1] || commits[index + 1]?.tag)) return false;
      return true;
    });
  });
}