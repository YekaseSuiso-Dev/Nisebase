import { c as createPluginWithCommands, d as defaultPlugin } from './shared/eslint-plugin-command.984d5c60.mjs';
import './shared/eslint-plugin-command.a8887319.mjs';

function config(options = {}) {
  const plugin = options.commands ? createPluginWithCommands(options) : defaultPlugin;
  const {
    name = "command"
  } = options;
  return {
    name,
    plugins: {
      [name]: plugin
    },
    rules: {
      [`${name}/command`]: "error"
    }
  };
}

export { config as default };
