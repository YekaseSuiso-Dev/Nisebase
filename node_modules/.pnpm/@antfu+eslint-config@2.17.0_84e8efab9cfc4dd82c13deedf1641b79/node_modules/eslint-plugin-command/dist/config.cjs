'use strict';

const index = require('./shared/eslint-plugin-command.cc55bfe8.cjs');
require('./shared/eslint-plugin-command.abbd2956.cjs');

function config(options = {}) {
  const plugin = options.commands ? index.createPluginWithCommands(options) : index.defaultPlugin;
  const {
    name = "command"
  } = options;
  return {
    name,
    plugins: {
      [name]: plugin
    },
    rules: {
      [`${name}/command`]: "error"
    }
  };
}

module.exports = config;
